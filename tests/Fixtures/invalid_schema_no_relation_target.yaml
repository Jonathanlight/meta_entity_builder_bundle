entities:
    User:
        table: users
        properties:
            id:
                type: integer
                id: true
        relations:
            posts:
                type: OneToMany
